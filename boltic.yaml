# boltic.yaml
app: "mas-sequential-thinking-mcp"
region: "asia-south1"

build:
  dockerfile: "Dockerfile"

env:
  LLM_PROVIDER: "deepseek"
  MCP_TRANSPORT: "streamable-http"   # switched from "http"
  # HOST and PORT are not needed for streamable-http; the SDK binds automatically
  # You can keep them if you want, but they will be ignored:
  # HOST: "0.0.0.0"
  # PORT: "8080"
  DEEPSEEK_API_KEY: "sk-ae7529c6aab24edab3606a168368dfad"
  EXA_API_KEY: "15b567d1-9af3-4ae3-bd49-e5d5bee228b6"

resources:
  size: "shared-cpu-1x"
  memory: "1024"
  max_memory: "2048"
